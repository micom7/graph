# Детальна інструкція з запуску (Windows 10/11)

> Усі команди виконуються у **PowerShell** або **cmd** від імені звичайного користувача.
> Проект розміщений у `d:\graph\`.

---

## Крок 0. Необхідне програмне забезпечення

### 0.1 Node.js (для фронтенду)

1. Завантажте **Node.js LTS** з https://nodejs.org/
   (станом на 2025 — версія 20 або 22)
2. Запустіть інсталятор, залиште всі налаштування за замовчуванням.
3. Перевірте встановлення — відкрийте нове вікно PowerShell:

```powershell
node --version   # має показати v20.x.x або вище
npm --version    # має показати 10.x.x або вище
```

---

### 0.2 Python 3.11+ (для бекенду)

1. Завантажте з https://www.python.org/downloads/
   Оберіть **Python 3.11** або вище.
2. **ВАЖЛИВО:** при встановленні поставте галочку
   **"Add Python to PATH"** (у самому низу першого екрана!).
3. Перевірте:

```powershell
python --version   # Python 3.11.x або вище
pip --version      # pip 23.x або вище
```

> Якщо команда `python` не знаходиться — спробуйте `py --version`.
> У цьому разі замінюйте `python` на `py` у всіх командах нижче.

---

### 0.3 PostgreSQL 16

1. Завантажте інсталятор з https://www.postgresql.org/download/windows/
   (EDB installer — рекомендований варіант)
2. Запустіть, у процесі встановлення:
   - Запам'ятайте або запишіть **пароль для користувача `postgres`**
   - Порт залиште **5432**
   - Locale: `Ukrainian, Ukraine` або `Default locale`
3. На останньому кроці **зніміть галочку** "Launch Stack Builder" — він не потрібен.
4. Після встановлення додайте `psql` до PATH:
   - Відкрийте **"Змінні середовища"** (`Win+R → sysdm.cpl → Додатково → Змінні середовища`)
   - У розділі **"Системні змінні"** знайдіть `Path`, натисніть **Змінити**
   - Додайте рядок: `C:\Program Files\PostgreSQL\16\bin`
   - Натисніть OK скрізь і **перезапустіть PowerShell**
5. Перевірте:

```powershell
psql --version   # psql (PostgreSQL) 16.x
```

---

## Крок 1. Налаштування бази даних

Відкрийте **новий** PowerShell і виконайте:

```powershell
# Підключитись до PostgreSQL як суперкористувач
psql -U postgres
```

Введіть пароль, який ви задали при встановленні. З'явиться запрошення `postgres=#`.

```sql
-- Створити базу даних для проекту
CREATE DATABASE graph_db;

-- Переконатись, що БД створена
\l

-- Вийти
\q
```

Тепер ініціалізуємо схему та наповнення:

```powershell
cd d:\graph

psql -U postgres -d graph_db -f backend/init.sql
```

Очікуваний вивід:
```
CREATE TABLE
CREATE TABLE
CREATE TABLE
CREATE TABLE
INSERT 0 6
```

Перевірити, що таблиці створились:

```powershell
psql -U postgres -d graph_db -c "\dt"
```

Має показати 4 таблиці: `connections`, `device_types`, `devices`, `ports`.

---

## Крок 2. Налаштування бекенду

### 2.1 Створити віртуальне середовище

```powershell
cd d:\graph\backend

# Створити venv
python -m venv venv

# Активувати venv (потрібно робити кожного разу при відкритті нового термінала)
venv\Scripts\activate
```

Після активації в рядку терміналу з'явиться `(venv)`:
```
(venv) PS d:\graph\backend>
```

### 2.2 Встановити залежності

```powershell
pip install -r requirements.txt
```

Очікується встановлення: `fastapi`, `uvicorn`, `asyncpg`, `pydantic`, `python-dotenv`.

### 2.3 Створити файл .env

```powershell
copy .env.example .env
```

Відкрийте `.env` (наприклад через `notepad .env`) і перевірте рядок:

```
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/graph_db
```

Замініть `postgres` (після двокрапки) на ваш реальний пароль від PostgreSQL.

Приклад, якщо пароль `mypassword123`:
```
DATABASE_URL=postgresql://postgres:mypassword123@localhost:5432/graph_db
```

### 2.4 Запустити бекенд

```powershell
uvicorn main:app --reload --port 8000
```

Очікуваний вивід:
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [...]
INFO:     Started server process [...]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

**Перевірка:** відкрийте у браузері http://localhost:8000/docs — має відкритись Swagger UI зі списком усіх ендпоінтів.

> Залиште це вікно терміналу відкритим — бекенд працює в ньому.

---

## Крок 3. Налаштування фронтенду

Відкрийте **нове** вікно PowerShell (бекенд продовжує працювати у старому):

```powershell
cd d:\graph\frontend

# Встановити npm-залежності (займає ~1-2 хвилини)
npm install

# Запустити dev-сервер
npm run dev
```

Очікуваний вивід:
```
  VITE v5.x.x  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

Відкрийте у браузері **http://localhost:5173**

---

## Крок 4. Перша робота з додатком

1. **Завантажити типи пристроїв і граф з БД**
   Натисніть кнопку **"☁️ Завантажити з БД"** внизу по центру.
   У лівій панелі з'являться кнопки типів пристроїв: Засувка, Норія, тощо.

2. **Додати вузол**
   Клікніть на будь-який тип у лівій панелі, наприклад **"+ Засувка"**.
   На полотні з'явиться вузол.

3. **Редагувати вузол**
   Двічі клікніть на вузол — відкриється права панель.
   Там можна:
   - Змінити назву пристрою
   - Додати вхідні (`+ Додати вхід`) і вихідні (`+ Додати вихід`) порти
   - Перейменувати порт (кнопка ✏️ або подвійний клік на назву)
   - Видалити порт або весь вузол

4. **З'єднати два вузли**
   Додайте другий вузол. Переконайтесь, що у першого є вихідний порт (зелений, праворуч), у другого — вхідний (блакитний, ліворуч).
   Наведіть мишу на зелений кружечок → курсор зміниться на хрестик → перетягніть до блакитного кружечка іншого вузла.

5. **Зберегти в БД**
   Натисніть **"💾 Зберегти в БД"**. Статус внизу зміниться на "Збережено в БД".

---

## Часті помилки

### `psql: error: connection to server ... failed`
- Переконайтесь, що служба PostgreSQL запущена:
  `Win+R → services.msc → знайдіть "postgresql-x64-16" → Запустити`

### `ModuleNotFoundError: No module named 'fastapi'`
- Ви забули активувати venv:
  ```powershell
  venv\Scripts\activate
  ```

### `asyncpg.exceptions.InvalidPasswordError`
- Невірний пароль у `.env`. Відредагуйте `d:\graph\backend\.env`.

### `CORS error` у браузері (фронтенд не може достукатись до API)
- Переконайтесь, що бекенд запущений на порту **8000**.
- Vite проксує `/api` → `localhost:8000` автоматично (налаштовано у `vite.config.ts`).

### `npm install` зависає або помилка мережі
- Спробуйте встановити через дзеркало:
  ```powershell
  npm install --registry https://registry.npmjs.org
  ```

### Порт 8000 або 5173 вже зайнятий
```powershell
# Знайти процес, що займає порт 8000
netstat -ano | findstr :8000
# Завершити процес (замінити PID на число зі списку)
taskkill /PID <PID> /F
```

---

## Зупинка серверів

- Бекенд: у терміналі де запущений uvicorn — натисніть `Ctrl+C`
- Фронтенд: у терміналі де запущений vite — натисніть `Ctrl+C`

---

## Повторний запуск (наступного разу)

```powershell
# Термінал 1 — бекенд
cd d:\graph\backend
venv\Scripts\activate
uvicorn main:app --reload --port 8000

# Термінал 2 — фронтенд
cd d:\graph\frontend
npm run dev
```

Відкрити http://localhost:5173
