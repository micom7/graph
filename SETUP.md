# Інструкція з запуску (Windows 10/11)

> Усі команди виконуються у **PowerShell** або **cmd**.
> Проект розміщений у `d:\graph\`.

---

## Варіант A: Docker (рекомендовано)

### Передумови
- Встановлений [Docker Desktop](https://www.docker.com/products/docker-desktop/)

### Перший запуск (збирає образ ~1-2 хв)

```powershell
cd d:\graph
docker compose up -d --build
```

Відкрити **http://localhost:5173**

### Наступні запуски

Двічі клікніть `start.bat` — контейнер стартує і браузер відкриється автоматично.

### Зупинка

Двічі клікніть `stop.bat`, або:
```powershell
docker compose down
```

### Після оновлення коду

```powershell
cd d:\graph
docker compose up -d --build
```

---

## Варіант B: без Docker (пряма розробка)

---

## Крок 0. Необхідне програмне забезпечення

### Node.js

1. Завантажте **Node.js LTS** з https://nodejs.org/
   (версія 20 або 22)
2. Запустіть інсталятор, залиште всі налаштування за замовчуванням.
3. Перевірте у новому вікні PowerShell:

```powershell
node --version   # v20.x.x або вище
npm --version    # 10.x.x або вище
```

Більше нічого встановлювати не потрібно — ні Python, ні PostgreSQL.

---

## Крок 1. Встановити залежності (лише перший раз)

```powershell
cd d:\graph\frontend
npm install
```

Очікується встановлення пакетів (~1 хвилина).

---

## Крок 2. Запустити додаток

```powershell
cd d:\graph\frontend
npm run dev
```

Очікуваний вивід:
```
  VITE v5.x.x  ready in 500 ms

  ➜  Local:   http://localhost:5173/
```

Відкрийте у браузері **http://localhost:5173**

Для зупинки — `Ctrl+C` у терміналі.

---

## Повторний запуск

```powershell
cd d:\graph\frontend
npm run dev
```

---

## Робота з файлами

- **"💾 Зберегти файл"** — завантажує `graph.json` у папку завантажень.
- **"📂 Відкрити файл"** — відкриває раніше збережений `graph.json`.
- Граф також автоматично зберігається у `localStorage` браузера — при наступному відкритті відновиться без зайвих дій.

> Рекомендується час від часу зберігати файл вручну як резервну копію.

---

## Часті помилки

### `npm install` зависає або помилка мережі

```powershell
npm install --registry https://registry.npmjs.org
```

### Порт 5173 вже зайнятий

```powershell
# Знайти процес
netstat -ano | findstr :5173
# Завершити (замінити PID на число зі списку)
taskkill /PID <PID> /F
```

### Браузер показує порожнє полотно після відкриття файлу

Переконайтесь, що файл має правильний формат (відкрийте у текстовому редакторі — має бути JSON з полями `devices` і `connections`).
